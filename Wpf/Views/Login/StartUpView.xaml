<UserControl x:Class="Wpf.Views.Login.StartUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">

            <Border Margin="10" BorderBrush="{DynamicResource AccentBaseColorBrush }" Background="{DynamicResource AccentBaseColorBrush}" BorderThickness="1"
                CornerRadius="100" Width="100" Height="100">
                <Ellipse>
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="../../Resources/Images/Restaurant/Logo.png" Stretch="UniformToFill" />
                    </Ellipse.Fill>
                </Ellipse>
            </Border>

            <TextBlock Margin="10" Style="{StaticResource TitleStyle}" Text="Restaurant System"/>

            <mah:MetroProgressBar  Margin="10" IsIndeterminate="True"/>

        </StackPanel>

    </Grid>


</UserControl>
